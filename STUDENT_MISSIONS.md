# 📜 ใบภารกิจ Git & GitHub: Fork, Branch, Sync Main & Pull Request

ใบภารกิจนี้แบ่งออกเป็น **2 ส่วนสำคัญ** ที่โปรแกรมเมอร์ใช้ทำงานจริงในทุกๆ วัน:

---

## 🗺️ แผนที่การเรียนรู้ (Curriculum Map)

```text
========================================================================
ส่วนที่ 1: ตะลุยโลก Open Source — ทำความรู้จักและฝึก Fork (20 นาที)
   ├── ทำความเข้าใจว่า Fork คืออะไร เมื่อไหร่ต้องใช้
   ├── ทดลองค้นหาหรือเลือก Fork จากตัวอย่าง Repo ยอดนิยม
   └── สำรวจผลลัพธ์บน GitHub (การเกิดสำเนาส่วนตัว และปุ่ม Sync fork)
========================================================================
ส่วนที่ 2: มหาศึกดันเจี้ยน Boss Raid — พัฒนาฟีเจอร์และรวมงาน (100 นาที)
   ├── ภารกิจ A: Clone โปรเจกต์เกมลงเครื่อง
   ├── ภารกิจ B: แตก Feature Branch & ปลุกพลังฮีโร่ (เขียน Logic + Asserts)
   ├── ภารกิจ C: Main Alert! ดึงโค้ดที่อัปเดตจาก main เข้ามาใน Feature Branch
   ├── ภารกิจ D: Push & เปิด Pull Request เข้า main พร้อมแก้ Conflict ในกิลด์
   └── ภารกิจ E: The Grand Boss Raid รวมพลังรันเกม & ส่องต้นไม้ประวัติ Git
========================================================================
```

---

# 🌐 ส่วนที่ 1: ตะลุยโลก Open Source ด้วยการ Fork (20 นาที)

### 1.1 Fork คืออะไร? ต่างจาก Clone อย่างไร?
- **Clone:** คือการคัดลอกไฟล์จาก GitHub ลงมาที่ **"เครื่องคอมพิวเตอร์ของเรา"**
- **Fork:** คือการคัดลอก Repository ของคนอื่นบน GitHub มาเป็น **"Repository ของเราเองบน GitHub"**
- **เมื่อไหร่ต้อง Fork?**
  - เมื่อเราอยากนำโค้ด Open Source ของคนอื่นมาดัดแปลงเป็นเวอร์ชันของตัวเอง
  - เมื่อเราไม่มีสิทธิ์แก้ไข (No Write Permission) ใน Repo นั้น แต่อยากช่วยแก้บั๊กแล้วส่งงานกลับไปให้เจ้าของ

### 1.2 ลงมือปฏิบัติ: เลือก Fork โปรเจกต์ที่สนใจ
ให้นักเรียนเปิดเบราว์เซอร์ เข้าสู่ GitHub และเลือก Fork โปรเจกต์ **1 โปรเจกต์** จากตัวอย่างด้านล่างนี้ (หรือค้นหาโปรเจกต์ที่ชอบเอง):

1. **GitHub Official Practice Repo:**  
   👉 [https://github.com/octocat/Spoon-Knife](https://github.com/octocat/Spoon-Knife) *(Repo ที่ GitHub สร้างไว้สำหรับฝึก Fork โดยเฉพาะ)*
2. **ตัวอย่างเกม 2D / Python ง่ายๆ:**  
   👉 [https://github.com/the-computer-scientist/Flappy-Bird-Python](https://github.com/the-computer-scientist/Flappy-Bird-Python)
3. **หรือค้นหาคำค้นหาที่ชอบ:** เช่น พิมพ์ค้นหา `python beginner game` บนแถบค้นหาของ GitHub

### 1.3 ขั้นตอนการ Fork
1. เข้าไปที่หน้า Repo ที่เลือก
2. สังเกตมุมขวาบน จะมีปุ่ม **"Fork"** พร้อมตัวเลขจำนวนคนที่เคย Fork ไปแล้ว
3. คลิกปุ่ม **Fork** -> ตรวจสอบชื่อ Repository Name -> กด **"Create fork"**
4. **สังเกตผลลัพธ์ที่เกิดขึ้น:**
   - ที่มุมซ้ายบน จะเปลี่ยนเป็น `<ชื่อบัญชีของคุณ> / <ชื่อโปรเจกต์>`
   - ใต้ชื่อโปรเจกต์จะมีข้อความสีเทาเขียนว่า: `forked from <ชื่อเจ้าของเดิม>`
   - สิทธิ์ใน Repo นี้กลายเป็นของคุณ 100%! คุณสามารถแก้โค้ดและ push อะไรก็ได้โดยไม่กระทบเจ้าของเดิม
5. *(ถ้าต้องการนำมาเล่นในเครื่อง)* สามารถกดปุ่ม `<> Code` แล้วคัดลอก URL เพื่อ `git clone` ลงเครื่องได้ตามปกติ

---

# 🐉 ส่วนที่ 2: มหาศึกดันเจี้ยน Boss Raid (100 นาที)

ในส่วนนี้ ทุกคนจะกลับมารวมตัวกันเป็นทีมกิลด์ **The Bug Hunters** เพื่อพัฒนาเกมตะลุยดันเจี้ยนร่วมกันใน Repository กลางของทีม!

---

## 🧭 แผนผังการทำงานของ Branch ในส่วนนี้

```text
[GitHub: Branch main] ──────── (ครูอัปเดตเกราะบอส) ──────────────────────────┐
      │ (Clone)                                                              │
      ▼                                                                      ▼
[เครื่องเรา: main] ──────► [แตก branch: feature/hero-xxx] ◄─── (merge เข้ามา)┘
                                  • เขียน Logic ฮีโร่
                                  • Self-test ด้วย assert
                                  • ใส่สโลแกนใน guild.py
                                           │
                                           ▼
                                [เปิด PR เข้า main & รวมพลังลุยบอส]
```

---

### ภารกิจ A: Clone โปรเจกต์เกมลงเครื่อง
1. รับลิงก์ GitHub Repo ของโปรเจกต์เกมจากคุณครู
2. เปิด Terminal ในเครื่องคอมพิวเตอร์ของคุณ แล้วรันคำสั่ง:
   ```bash
   git clone <URL-REPO-เกมของครู> boss-raid
   cd boss-raid
   git status
   ```
   *(คุณจะอยู่บน branch `main`)*

---

### ภารกิจ B: แตก Feature Branch & ปลุกพลังฮีโร่
1. สร้าง Branch ใหม่สำหรับการพัฒนาฮีโร่ของตนเอง:
   ```bash
   git switch -c feature/hero-<ชื่อเล่นภาษาอังกฤษของคุณ>
   ```
   *ตัวอย่าง: `git switch -c feature/hero-alex`*

2. เปิดไฟล์ **[HERO_ROLES.md](HERO_ROLES.md)** เพื่อเลือก 1 อาชีพที่สนใจ (ไม่ซ้ำกับเพื่อนในทีม)
3. สร้างไฟล์ในโฟลเดอร์ `heroes/` (เช่น `heroes/hero_alex.py`)
4. คัดลอก **โครงโค้ดตั้งต้น** จาก [HERO_ROLES.md](HERO_ROLES.md) มาวาง แล้ว **เขียนเงื่อนไข `if - elif - else` 3 ข้อด้วยตนเอง** ตรงส่วน `# TODO`
5. ทดสอบรันไฟล์ของตนเอง:
   ```bash
   python heroes/hero_alex.py
   ```
   *ต้องขึ้น `✅ ... tests passed!` โดยไม่มี AssertionError*

6. บันทึก Commit ไฟล์ฮีโร่:
   ```bash
   git add heroes/
   git commit -m "feat: Add Alex the Berserker Hero"
   ```

7. เปิดไฟล์ `guild.py` แล้วทำ 3 สิ่ง:
   - เปลี่ยน `GUILD_SLOGAN` ให้เป็นสโลแกนในใจคุณ
   - เพิ่มคำสั่ง `import` ฮีโร่ของคุณ
   - เพิ่มชื่อฮีโร่ของคุณลงใน `PARTY_MEMBERS`
8. Commit ไฟล์ `guild.py`:
   ```bash
   git add guild.py
   git commit -m "feat: Register Alex to the guild party"
   ```

---

### ภารกิจ C: Main Alert! ดึงโค้ดที่อัปเดตจาก main เข้า Feature Branch
> 🚨 **สถานการณ์ฉุกเฉิน:** คุณครูเพิ่งประกาศว่ามังกรได้ปลุกพลังเกราะเวทมนตร์ขึ้นมา และได้ push โค้ดระบบเกราะเข้า branch `main` แล้ว!  
> ตอนนี้โค้ดบน branch `main` ล้ำหน้ากว่า branch ของเรา เราต้องดึงอัปเดตจาก `main` เข้ามาผสานกับ `feature branch` ที่เรากำลังทำอยู่ทันที!

1. ดึงข้อมูลล่าสุดจาก GitHub:
   ```bash
   git fetch origin
   ```

2. ตรวจสอบว่าตัวเองยังคงอยู่ที่ feature branch ของตนเอง:
   ```bash
   git branch
   ```
   *(ต้องมีเครื่องหมาย `*` อยู่หน้า `feature/hero-...`)*

3. ทำการ **Merge โค้ดล่าสุดจาก `main` เข้ามาที่ feature branch ของเรา**:
   ```bash
   git merge origin/main
   ```
   *(ไฟล์ `main.py` ในเครื่องของเราจะได้รับระบบเกราะบอสใหม่ทันที โดยที่โค้ดฮีโร่ของเราไม่สูญหาย!)*

---

### ภารกิจ D: Push & เปิด Pull Request เข้าสู่ Branch หลัก
1. ส่ง Branch ของเราขึ้น GitHub:
   ```bash
   git push -u origin feature/hero-<ชื่อเล่นของคุณ>
   ```

2. เข้าไปที่หน้า GitHub ของโปรเจกต์ กดปุ่ม **"Compare & pull request"**
3. ตรวจสอบว่า:
   - **Base:** `main` (Branch หลักของทีม)
   - **Compare:** `feature/hero-<ชื่อเล่นของคุณ>` (Branch ของเรา)
4. ตั้งชื่อ PR อธิบายความสามารถฮีโร่ของคุณ แล้วกด **"Create pull request"**

#### 💥 การเจรจาแก้ Conflict ใน `guild.py`:
เมื่อเพื่อนในทีมส่ง PR เข้ามาพร้อมๆ กัน และครูกดรับ PR คนแรก คนที่เหลืออาจจะติด Conflict ในบรรทัดสโลแกน `GUILD_SLOGAN` ใน `guild.py`:
```text
<<<<<<< main
GUILD_SLOGAN = "ดาบแห่งแสงจะฟันบอสให้ขาดสะบั้น!"
=======
GUILD_SLOGAN = "พวกเราจะล่าบั๊ก พิชิตมังกรด้วยพลังเวท!"
>>>>>>> feature/hero-alex
```
1. หันไปคุยกับเพื่อนที่เขียนชนกัน: เลือกว่าจะใช้ข้อความของใคร หรือรวมข้อความเข้าด้วยกัน
2. ลบเครื่องหมาย `<<<<<<<`, `=======`, `>>>>>>>` ออก เหลือเฉพาะบรรทัดที่ตกลงกัน
3. บันทึกและ Commit บน GitHub เพื่อให้ PR ขึ้นสถานะพร้อม Merge!

---

### ภารกิจ E: The Grand Boss Raid & ส่องประวัติศาสตร์
เมื่อ PR ของทุกคนถูก Merge รวมเข้าสู่ `main` ครบถ้วนแล้ว:

1. สลับกลับมาที่ branch `main` ในเครื่อง และดึงโค้ดเวอร์ชันสมบูรณ์:
   ```bash
   git switch main
   git pull origin main
   ```
2. ตรวจสอบใน VS Code จะเห็นไฟล์ฮีโร่ของเพื่อนทุกคนมารวมตัวกันในโฟลเดอร์ `heroes/`!
3. รันมหาศึกตีบอสพร้อมกัน:
   ```bash
   python main.py
   ```
   *(ลุ้นผลดาเมจและดูว่าใครได้ตำแหน่ง MVP ประจำกิลด์!)*

4. ดูแผนผังการร่วมมือของทุกคนผ่านประวัติ Git:
   ```bash
   git log --oneline --graph --all -15
   ```
